<% if flash[:msg] %>
<div id="mensagem">
	<h2><%=flash[:msg]%></h2>
</div>
<% end %>

<% form_for ([@Group, @Contact]) do |f| %>
	<%=f.error_messages%>
	<label>
		Contato(s):<br>
		<%=f.text_area :email %><br>
		<br>
		<i>*separe os e-mails por , (virgula)</i>
	</label><br><br>
	
	<button type="submit">Salvar</button>
<% end %>
